<li>
  <!-- <span class="content"><%= micropost.content %></span> -->
   <div class="container">
  		<div class="row">
  			<div class="span6">
  				<p><strong>Uploaded <%= time_ago_in_words(micropost.created_at) %> ago.</strong></p>
  				<%= image_tag(micropost.image_url, :size => "600x400") %>
  				<% if current_user?(micropost.user) %>
  				    <%= link_to "delete", micropost, method: :delete,
  				                                     data: { confirm: "You sure?" },
  				                                     title: micropost.content %>
  				  <% end %>
  			</div>
		</div>
	</div>
</li>