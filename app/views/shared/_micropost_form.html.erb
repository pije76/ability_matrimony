<%= form_for(@micropost, :html => {:multipart => true}) do |f| %>
  
  <div class="field">
    <!-- <%= f.text_area :content, placeholder: "Compose new micropost..." %> -->
    <h3>Upload Your Photo*</h3><%= f.file_field :image %>
  </div>
  <%= f.submit "Upload", class: "btn btn-large btn-primary" %>
<% end %>